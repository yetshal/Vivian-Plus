# 🚀 Archivo .gitignore para Vivian-Plus
# Ignora archivos/directorios comunes en la raíz, backend (Node.js/Express) y frontend (Angular)

# --- Archivos Comunes ---
# Ignorar logs, archivos de sistema y temporales
*.log
.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/ # Directorio de configuración de IDEs (IntelliJ/WebStorm)
.vscode/ # Directorio de configuración de VS Code (a menudo se ignora)

# --- Backend (Node.js/Express) ---
/backend/node_modules
/backend/dist
/backend/build
/backend/coverage
# Variables de entorno
#/backend/.env
#/backend/.env.*
# Archivos de dependencia bloqueados que no necesitan ser versionados si usas npm o yarn
/backend/package-lock.json
/backend/yarn.lock

# --- Frontend (Angular 18) ---
/frontend/node_modules
# Directorios de compilación y salida (dist)
/frontend/dist/
/frontend/out/
# Cachés de compilación de Angular
/frontend/.angular/cache/
# Variables de entorno
/frontend/.env
/frontend/.env.*
# Archivos de depuración y cobertura de pruebas
/frontend/coverage/
# Archivos de dependencia bloqueados que no necesitan ser versionados
/frontend/package-lock.json
/frontend/yarn.lock